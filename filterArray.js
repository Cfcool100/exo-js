function filterArray(arr) {

    for (let i = 0; i < arr.length; i++) {
        if (arr[i] === number) {
            arr[i];
        }
    }
    return arr[i];
}
console.log(filterArray([1, "re", 0, 2, "pq", 10]));
